<p id="notice"><%= notice %></p>

<% content_for :title, translate_model(Reproducibility) %>

<div class="dataTables_wrapper dt-bootstrap4">  
  <div calss="row">
    <div calss="col-sm-12">      
      <table id="reproducibility_table" class="table table-bordered table-striped dtr-inline" role="grid">
        <thead>
          <tr role="row">
            <th width="3%">#</th>
            <th class="sorting"> <%= translate_attribute(Reproducibility, :description) %> </th>        
            <th class="sorting text-center" width="11%"> <%= translate_attribute(Reproducibility, :created_at) %> </th>
            <th class="text-center" width="10%"> <%= t('others.actions') %> </th>
          </tr>
        </thead>

        <tbody>
          <% @reproducibilities.each do |reproducibility| %>
            <tr>
              <td><%= reproducibility.id %></td>
              <td><%= reproducibility.description %></td>      
              <td><%= l(reproducibility.created_at) %></td>

              <td>
                <div class="text-center">
                  <%= link_to reproducibility, class: "btn btn-outline-secondary btn-sm" do %>                    
                      <span class="fa fa-search" />                      
                  <% end %>

                  <%= link_to edit_reproducibility_path(reproducibility), class: "btn btn-outline-secondary btn-sm" do %>                    
                      <span class="fa fa-pen" />
                  <% end %>

                  <%= link_to reproducibility, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-outline-secondary btn-sm" do %>
                      <span class="fa fa-trash" />                         
                  <% end %>
                </div>
              </td>
              
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<div>
  <%= link_to t('buttons.new') + ' ' + translate_model(Reproducibility), new_reproducibility_path, class: "btn btn-primary"  %>
</div>
              