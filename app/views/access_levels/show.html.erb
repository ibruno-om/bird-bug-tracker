<p id="notice"><%= notice %></p>

<table class="table">
  <tr>
    <td colspan="5">
      <b><%= translate_attribute(AccessLevel, :description) %>:</b>
      <%= @access_level.description %>
    </td>
  </tr>
</table>

<div>
  <%= link_to t('buttons.edit') , edit_access_level_path(@access_level)  %> |
  <%= link_to t('buttons.back') , access_levels_path %>
</div>

<br/>